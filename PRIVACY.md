# 隐私与本机数据说明

适用版本：AI接入助手 `0.12.0 (231)` source-only 连续源码快照（非 tag、非 Release）。

## 结论

AI接入助手默认在本机处理数据，不设应用内遥测、广告追踪或云端会话索引。普通启动、列表展示和浏览不会自动发起模型请求。联网检查、可能收费的真实任务、配置修改、诊断导出和更新检查都必须由用户明确触发。

macOS、Codex Desktop、模型服务商和用户主动打开的网站有各自的数据处理规则，不属于本应用控制范围。

## 本机读取

- 读取 Codex 与应用自身版本、运行状态、受管配置结构和脱敏回执，用于判断当前能否工作。
- 历史页默认读取本机 SQLite 元数据，例如标题、时间、工作目录、来源和归档状态；普通浏览不扫描会话正文，也不建立云端索引。
- 最近完成请求的 token、时间和价格快照来自本机 Codex 结构化事件；任务 ID 先做不可逆摘要，不保存或显示提示词、响应正文和工具内容。
- 官方额度只在用户点击后通过本机受信 Codex 接口读取；应用不读取 `auth.json`，不刷新登录令牌。

## 凭据

中转凭据保存在应用自有 macOS Keychain 命名空间。界面状态不读取凭据正文；只有用户明确执行连接或真实任务时，受控流程才读取对应凭据。凭据、Cookie、认证头和 Keychain 引用不进入诊断包、迁移清单或公开价格检查。

## 联网与费用

- 添加、检查或验证中转时，只在用户确认后访问对应地址。
- 基础连接和真实任务是两个动作；任何可能产生费用的请求都要求本次确认。
- 应用不后台重试、不自动切轨，也不把连接成功写成真实任务完成。
- 更新检查默认关闭；即使清单验证通过，也只提示并等待用户确认，不自动安装或重启。

## 本机写入与导出

- 保存的中转资料、脱敏回执、恢复记录、收藏和应用状态写入应用管理的本机存储。
- 配置或会话变更先预览并确认，再按受管字段原子写入；认证文件和未受管配置不属于普通切轨写入范围。
- 诊断、求助包、配置快照和迁移设置只在用户选择位置后导出；取消时不写文件。
- 导出固定排除 API Key、Token、Authorization/Cookie、提示词、请求与响应正文、工具输入输出、会话正文、真实项目路径、自定义中转名称和地址、原始错误文本。
- 导出不会自动上传；发送前仍应由用户查看内容。

## 保留与删除

- 用户可在应用内删除已保存中转；删除凭据需要明确确认。
- 用户导出的文件由用户自行保留或删除。
- 卸载应用不承诺自动删除 Codex 自有数据、Keychain 条目或用户导出文件。
- 恢复点和失败候选不会静默删除；应用先给出候选，再等待用户决定。

## 可核查范围

本说明描述源码合同，不代表第三方服务的隐私承诺，也不构成法律合规认证。许可证、对应源码和第三方来源分别见 `LICENSE`、`SOURCE-CODE.md` 与 `THIRD_PARTY_NOTICES.md`。
