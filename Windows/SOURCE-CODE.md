# Windows 对应源码边界

本目录包含 Windows 11 x64 原生应用、核心合同测试和只读源码核验脚本。目标工具链为 .NET 10 LTS、WinUI 3 和 `win-x64`。

本 source-only preview 不含 MSIX、Appx、ZIP、Setup、DMG 或其他 Windows 二进制，也不含私有 CI 历史或内部发布回执。安装器与公开分发门留待独立 Windows 11 用户运行和签名验收。
